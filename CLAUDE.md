See [AGENTS.md](AGENTS.md) — the database schema and the access model.

Note: this database is no longer readable or writable from outside the app.
There is no public read URL and no agent account. Food goes in through the
in-app estimator, or by pasting JSON into Fuel → ⚙ → Paste food JSON.
