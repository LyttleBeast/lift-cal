# Secrets. Nothing in this repo needs a key — the Worker and its .dev.vars live
# in ~/dev/rack-worker — but GitHub Pages serves every file here to every
# visitor, so the belt stays on.
.dev.vars
.env

# Tooling
node_modules/
.wrangler/
dist/

# OS junk
.DS_Store
Thumbs.db
